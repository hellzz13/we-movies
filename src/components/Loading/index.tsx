import React from "react";
import { LoadingContainer, Spinner } from "./style";

export default function Loading() {
  return (
    <LoadingContainer>
      <Spinner />
    </LoadingContainer>
  );
}
